server {
        listen      80;
        server_name test.dev-test.online;
        
        location /.well-known/acme-challenge/ {
            root /var/www/certbot;
        }

        location / {
            include proxy_params;
            proxy_pass http://34.87.158.114:5000;
        }
}